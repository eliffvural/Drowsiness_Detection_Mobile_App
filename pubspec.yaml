name: drowsiness_detection_app
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  tflite: ^1.1.2
  just_audio: ^0.9.34
  google_ml_kit: ^0.16.0

flutter:
  assets:
    - assets/cnncat2.tflite
    - assets/alarm.wav